// assets/js/main.js
// NeoBrutality UI Kit - Basic JS

document.addEventListener("DOMContentLoaded", () => {
  console.log("🎨 NeoBrutality UI Kit v1.2.0 loaded!");

  // Пример: переключение темы (Q)
  /*
	const themeToggle = document.createElement('button');
	themeToggle.className = 'nb-btn nb-btn-outline';
	themeToggle.textContent = 'Тема';
	themeToggle.style.position = 'fixed';
	themeToggle.style.bottom = '20px';
	themeToggle.style.right = '20px';
	themeToggle.onclick = () => document.body.classList.toggle('nb-dark');
	document.body.appendChild(themeToggle);
	*/
});
