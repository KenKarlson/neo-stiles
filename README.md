# neobrutality-ui v1.2.0

Ретро-фреймворк в стиле комиксов.

## 📁 Структура

```
project/
├── index.html                    # Информация с демо-элементами
├── assets/
│   ├── css/neobrutality.css     # Основной CSS-фреймворк
│   ├── js/main.js               # Базовый JS
│   ├── img/                     # Папка для изображений
│   └── sass/                    # Папка для SCSS-исходников
└── README.md
```

## 🚀 Быстрый старт

1. Открой `index.html` в браузере — увидишь документацию со всеми компонентами
2. Скачай `assets/css/neobrutality.css` через кнопку на странице
3. Подключи в своём проекте:
   ```html
   <link rel="stylesheet" href="assets/css/neobrutality.css">
   ```
4. Используй классы с префиксом `nb-`:
   ```html
   <button class="nb-btn nb-btn-primary">Кнопка</button>
   <div class="nb-card">...</div>
   ```

## 🎨 Компоненты

| Компонент | Классы | Описание |
|-----------|--------|----------|
| Кнопки | `.nb-btn`, `.nb-btn-primary`, `.nb-btn-success` | Ретро-кнопки с жесткой тенью |
| Карточки | `.nb-card`, `.nb-card-body`, `.nb-card-title` | Контейнеры для контента |
| Формы | `.nb-form-control`, `.nb-form-group` | Стилизованные инпуты |
| Сетка | `.nb-row`, `.nb-col-4`, `.nb-col-6` | Flexbox-сетка |
| Навигация | `.nb-navbar`, `.nb-navbar-brand` | Шапка сайта |
| Уведомления | `.nb-alert-success`, `.nb-alert-danger` | Блоки сообщений |
| Паттерны | `.nb-bg-dots`, `.nb-bg-grid`, `.nb-bg-dots-fade` | Комикс-фоны |

## 🌓 Темы

Переключай тему, добавив класс `nb-dark` на `<body>`:

```html
<body class="nb-dark">
```

## 🎭 Фоновые паттерны

```html
<!-- Точки с затуханием на желтом фоне -->
<section class="nb-bg-pattern nb-bg-dots-fade nb-bg-pattern-warning">
  <h1>Заголовок</h1>
</section>

<!-- Сетка на белом фоне -->
<div class="nb-card nb-bg-pattern nb-bg-grid nb-bg-pattern-surface">
  <div class="nb-card-body">Контент</div>
</div>
```

Доступные паттерны:
- `nb-bg-dots` — равномерные точки
- `nb-bg-dots-fade` — точки с затуханием слева направо
- `nb-bg-dots-fade-down` — точки с затуханием сверху вниз
- `nb-bg-grid` — квадратная сетка
- `nb-bg-big-dots` — крупные точки (поп-арт)
- `nb-bg-crosshatch` — диагональная штриховка
- `nb-bg-lines` — горизонтальные линии

## ⚙️ CSS-переменные

Настрой стиль под себя:

```css
:root {
  --nb-border-width: 3px;      /* Толщина рамок */
  --nb-shadow-offset: 5px;     /* Размер тени */
  --nb-radius: 4px;            /* Скругление */
  --nb-primary: #a855f7;       /* Основной цвет */
}
```

## 📄 Лицензия
#License: Look but don't touch [=^_^=]
MIT — используй как хочешь!
